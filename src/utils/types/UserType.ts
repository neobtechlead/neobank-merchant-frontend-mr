export type UserType = {
    externalId?: string,
    firstName?: string,
    lastName?: string,
    email?: string,
    verificationToken?: string,
    accessKey?: string,
    authToken?: string,
    roles?: string[],
}