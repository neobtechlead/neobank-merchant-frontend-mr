export type UserType = {
    id?: string,
    firstName?: string,
    lastName?: string,
    email?: string,
    verificationToken?: string,
    accessKey?: string,
    authToken?: string,
    roles?: string[],
}