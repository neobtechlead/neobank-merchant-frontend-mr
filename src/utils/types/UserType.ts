export type UserType = {
    id?: string,
    firstName?: string,
    lastName?: string,
    email?: string,
    verificationToken?: string,
    accessKey?: string,
    token?: string,
    roles?: string[],
}