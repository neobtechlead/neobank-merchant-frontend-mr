export type TransactionType = {
    id?: string,
    date?: string,
    time?: string,
    recipient?: string,
    client?: string,
    batchNumber?: string,
    type?: string,
    amount?: string,
    status?: string,
    phone?: string,
    reference?: string,
    email?: string,
    channel?: string,
    sender?: string,
    balance?: string,
    description?: string,
    scheduled?: boolean
}